#!/bin/env python

class guy{
	def __init__(self,pos,hp,att):
		self.pos = pos
		self.hp = hp
		self.att = att
	def move(self,m):
	
	def attack(self,guys){
	
	def adj(self,mec):
		return (abs(self.pos[0]-mec.pos[0]) == 1 and self.pos[1] == mec.pos[1]) != (abs(self.pos[1]-me	





